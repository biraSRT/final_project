import styled from "styled-components";

const SignIn = () => {
    return(
        <Wrapper>
            <h1>Sign In</h1>
        </Wrapper>
    )
};

const Wrapper = styled.div`
    height: 100vh;
    width: 100%;
`;

export default SignIn;